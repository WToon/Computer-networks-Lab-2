TODO
- RequestParser Support different statuscodes



DONE
- ResponseParser support gif images + images found externally
- ResponseParser take into account charsets when reading html (add charsets to switch)
- ResponseParser Save the images in appropriate locations (read paths better)
- Request implement Request-constructor with content body for PUT and POST requests.
- ResponseParser still assumes a content body. Add boolean and set after parsing headers.
